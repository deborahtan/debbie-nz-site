# debbie.nz — Global Data Dictionary (v1)

Generated by the measurement-plan tool. This is the source of truth for
every event, parameter, and variable running on debbie.nz. Update this file
first when adding new tracking — GTM configuration should always trace back
to a line here.

---

## Events

| Event name | GA4 type | Fires when | Status |
|---|---|---|---|
| `search` | Custom (mirrors GA4 recommended `search`) | Search bar submitted (Enter key) | Live |
| `mission_assigned` | Custom | Immediately after `search`, once mission is resolved | Live |
| `shop_teaser_click` | Custom, marked as conversion | "Shop" nav link clicked | Live |
| `post_click` | Custom | A post card clicked on the homepage grid | Live |
| `page_view` | GA4 automatic | Every page load | Live (automatic) |

## Parameters

| Parameter | Attached to | Scope | Values |
|---|---|---|---|
| `search_term` | `search` | Event | raw search string, lowercase-normalised in GTM |
| `visitor_mission` | `mission_assigned` | **User** (see GA4 admin note below) | `practitioner_researcher` / `ai_thought_leadership` / `art_creative` / `consulting_prospect` / `community_event` / `unclassified` |
| `post_id` | `post_click` | Event | slug string, e.g. `ai-crawlers` |
| `content_group` | `post_click` | Event | `blog` (only value in use today; reserved for `shop`, `about` later) |

## GA4 admin setup (manual — not part of GTM import)

These two steps happen in the GA4 property itself, not GTM, and only need
doing once:

1. **Admin → Custom definitions → Create custom dimension**
   Dimension name: `Visitor Mission`
   Scope: **User**
   Event parameter: `visitor_mission`

2. **Admin → Events → mark `shop_teaser_click` as a conversion**
   (Wait for it to appear in the Events list after first real data lands,
   usually within 24 hours of the tag firing.)

## Mission lookup table (source of truth for the GTM Lookup Table variable)

| Search term contains | Mission value |
|---|---|
| gtm, bigquery, server-side, measurement, tracking, data layer | `practitioner_researcher` |
| ai, llm, claude, agentic, prompt | `ai_thought_leadership` |
| art, painting, lino, zine, watercolour, tattoo | `art_creative` |
| consulting, bytesized, hire, engagement, rates | `consulting_prospect` |
| measurecamp, auckland, event, conference | `community_event` |
| *(no match)* | `unclassified` |

---

## Change log

- **v1** — initial dictionary, mission scoring via GTM Lookup Table
  (keyword match, no BigQuery/ML yet — see personalization-demo-plan.md
  for the next-phase trigger criteria)
